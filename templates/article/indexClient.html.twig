{% extends 'front.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    {# <h1>Article index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idarticle</th>
                <th>Titre</th>
                <th>Contenu</th>
                <th>Description</th>
                <th>Nbrlike</th>
                <th>User</th>
              
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.idarticle }}</td>
                <td>{{ article.titre }}</td>
                <td>{{ article.contenu }}</td>
                <td>{{ article.description }}</td>
                <td>{{ article.nbrlike }}</td> 
                <td>{{article.idUser}}
               
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}
        {% for article in articles %}

    <div class="wrapper">
  <div class="blog_post">
      <h3>{{ article.titre }}</h3>
      <h1>{{ article.description }}</h1>
      <p>{{ article.contenu }}.</p>
      <span>{{ article.nbrlike }} likes</span>
      <br>
      <br>

    <a class="btn_primary" href='#'>Read More</a>
  </div>
</div>
        {% endfor %}

{% endblock %}
