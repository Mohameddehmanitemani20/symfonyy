{% extends 'back.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idarticle</th>
                <td>{{ article.idarticle }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.contenu }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Nbrlike</th>
                <td>{{ article.nbrlike }}</td>
            </tr>
        </tbody>
    </table>

     <a class="btn btn-success" href="{{ path('app_article_edit', {'idarticle': article.idarticle}) }}">edit</a><br><br>

    <a class="btn btn-primary w=100" href="{{ path('app_article_index') }}">back to list</a><br><br>

    

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}

